<section>
    <h2>You are viewing {{restaurants.name}} restaurants</h2>
        <article>
            <img src="{{restaurants.image}}"/>
            <h2>{{restaurants.name}}</h2>
        </article>
</section>